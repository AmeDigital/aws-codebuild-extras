version: 0.2

phases:
  install:
    commands:
      - curl -fsSL https://raw.githubusercontent.com/AmeDigital/aws-codebuild-extras/master/install >> extras.sh
      - . ./extras.sh
  build:
    commands:
      - # Do nothing
